# Pinned to wherever this was initialised from, so the repo can live
# alongside other projects rather than under ~/.local/share.
sourceDir = {{ .chezmoi.sourceDir | quote }}

[data]
    name = "Jamie van Dyke"
    email = "me@fearof.fish"

[edit]
    command = "nvim"

[git]
    autoCommit = false
    autoPush = false
