# The global excludes file ignores CLAUDE.md so personal agent instructions
# never land in project repos. This repo is where it is meant to live.
!dot_claude/CLAUDE.md

# Debug logs that land in the repo root by accident
even-terminal*.log
